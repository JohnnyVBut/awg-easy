# 🔐 Решение проблемы с паролем AWG-Easy

## ❌ Проблема

Пароль не подходит при входе в Web UI, хотя вы уверены что вводите правильный пароль.

## 🔍 Причина

Bcrypt хеш пароля содержит символ `$`, который нужно правильно экранировать в разных контекстах:

**Пример bcrypt хеша:**
```
$2y$05$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W
```

Обратите внимание на символы `$` - они имеют специальное значение в shell и docker-compose!

---

## ✅ Решения

### Вариант 1: Использовать скрипт генерации (рекомендуется)

```bash
# 1. Сгенерировать хеш
./generate-password.sh

# Скрипт покажет:
# - Обычный хеш для docker run
# - Экранированный хеш для docker-compose.yml

# 2. Скопировать нужный вариант в конфиг
```

---

### Вариант 2: Docker Compose (экранирование $$)

**В docker-compose.yml используйте двойной `$$`:**

```yaml
environment:
  # НЕПРАВИЛЬНО ❌
  - PASSWORD_HASH=$2y$05$Gj8nULE.hqMWx...
  
  # ПРАВИЛЬНО ✅
  - PASSWORD_HASH=$$2y$$05$$Gj8nULE.hqMWx...
```

**Как сделать:**

1. Сгенерировать хеш:
```bash
# wgpw выводит: PASSWORD_HASH='$2y$...'
# Нужно извлечь только хеш
WGPW_OUTPUT=$(docker run --rm awg-easy:2.0 wgpw 'your-password')
HASH=$(echo "$WGPW_OUTPUT" | grep -oP "PASSWORD_HASH='\K[^']+")

echo $HASH
```

Вывод:
```
$2y$12$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W
```

2. Заменить все `$` на `$$`:
```
$$2y$$05$$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W
```

3. Вставить в docker-compose.yml:
```yaml
- PASSWORD_HASH=$$2y$$05$$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W
```

---

### Вариант 3: Docker Run (одинарные кавычки)

**В docker run используйте одинарные кавычки:**

```bash
# НЕПРАВИЛЬНО ❌
docker run -e PASSWORD_HASH="$2y$05$Gj8nULE..." awg-easy:2.0

# ПРАВИЛЬНО ✅
docker run -e PASSWORD_HASH='$2y$05$Gj8nULE...' awg-easy:2.0
```

**Полный пример:**

```bash
# Генерация (правильный парсинг!)
WGPW_OUTPUT=$(docker run --rm awg-easy:2.0 wgpw 'your-password')
HASH=$(echo "$WGPW_OUTPUT" | grep -oP "PASSWORD_HASH='\K[^']+")

# Использование (обратите внимание на одинарные кавычки!)
docker run -d \
  -e PASSWORD_HASH=''"$HASH"'' \
  awg-easy:2.0
```

---

### Вариант 4: Использовать обновленный run.sh

Скрипт `run.sh` уже исправлен и правильно экранирует пароль:

```bash
./run.sh
```

Он автоматически:
1. Запросит пароль
2. Сгенерирует хеш
3. Правильно передаст его в контейнер с экранированием

---

## 🧪 Проверка

### Как проверить что хеш передан правильно:

```bash
# Посмотреть переменные окружения контейнера
docker exec awg-easy printenv PASSWORD_HASH
```

**Должно вывести:**
```
$2y$05$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W
```

**НЕ должно быть:**
- Пустая строка
- Обрезанный хеш
- Хеш с ошибками

---

## 📝 Примеры с разными паролями

### Пример 1: Простой пароль

**Пароль:** `mypassword`

```bash
# Генерация (правильный способ)
WGPW_OUTPUT=$(docker run --rm awg-easy:2.0 wgpw 'mypassword')
HASH=$(echo "$WGPW_OUTPUT" | grep -oP "PASSWORD_HASH='\K[^']+")

# Вывод (пример):
$2y$12$vQN1ZqB8mHzTYmEfPxLXAeX5J7d.3K9FgHb2wPqR5sNmT8uVwXyZC

# Для docker-compose.yml:
$$2y$$12$$vQN1ZqB8mHzTYmEfPxLXAeX5J7d.3K9FgHb2wPqR5sNmT8uVwXyZC

# Для docker run:
-e PASSWORD_HASH='$2y$12$vQN1ZqB8mHzTYmEfPxLXAeX5J7d.3K9FgHb2wPqR5sNmT8uVwXyZC'
```

### Пример 2: Сложный пароль

**Пароль:** `MyS3cur3P@ssw0rd!`

```bash
# Генерация (правильный способ)
WGPW_OUTPUT=$(docker run --rm awg-easy:2.0 wgpw 'MyS3cur3P@ssw0rd!')
HASH=$(echo "$WGPW_OUTPUT" | grep -oP "PASSWORD_HASH='\K[^']+")

# Вывод (пример):
$2y$12$8KfR3nQpX2vYmWzE5tJhLeL9P4sD6cNbM7wVxGfHq1jTkUiOpAaBq

# Для docker-compose.yml:
$$2y$$12$$8KfR3nQpX2vYmWzE5tJhLeL9P4sD6cNbM7wVxGfHq1jTkUiOpAaBq

# Для docker run:
-e PASSWORD_HASH='$2y$12$8KfR3nQpX2vYmWzE5tJhLeL9P4sD6cNbM7wVxGfHq1jTkUiOpAaBq'
```

# Для docker run:
-e PASSWORD_HASH='$2y$05$8KfR3nQpX2vYmWzE5tJhLeL9P4sD6cNbM7wVxGfHq1jTkUiOpAaBq'
```

---

## 🔧 Быстрое исправление

Если контейнер уже запущен с неправильным паролем:

```bash
# 1. Остановить и удалить контейнер
docker stop awg-easy
docker rm awg-easy

# 2. Запустить с правильным паролем
./run.sh
# или
docker-compose up -d  # (если исправили docker-compose.yml)
```

---

## 📚 Шпаргалка

| Контекст | Как экранировать |
|----------|------------------|
| **docker-compose.yml** | Заменить `$` на `$$` |
| **docker run (переменная)** | Использовать `'$HASH'` с одинарными кавычками |
| **docker run (прямо)** | Использовать `-e PASSWORD_HASH='$2y$...'` |
| **.env файл** | Не нужно экранировать |
| **bash скрипт** | `"'$HASH'"` или `'$HASH'` |

---

## ✅ Рекомендации

1. **Используйте `./run.sh`** - он уже всё делает правильно
2. **Или используйте `./generate-password.sh`** - он покажет оба варианта
3. **Проверяйте** хеш после запуска: `docker exec awg-easy printenv PASSWORD_HASH`
4. **Сохраните** хеш в безопасное место для будущих деплоев

---

## 🐛 Troubleshooting

### Симптом: "Invalid credentials"

**Причина:** Хеш неправильно экранирован

**Решение:**
```bash
# Проверить хеш в контейнере
docker exec awg-easy printenv PASSWORD_HASH

# Если пустой или неправильный - пересоздать контейнер
docker stop awg-easy
docker rm awg-easy
./run.sh
```

### Симптом: Хеш обрезан

**Причина:** Символ `$` не был экранирован, shell интерпретировал его как переменную

**Пример:**
```bash
# Неправильно - хеш обрезается
echo $2y$05$abc  # Выведет: y

# Правильно
echo '$2y$05$abc'  # Выведет: $2y$05$abc
```

**Решение:** Используйте одинарные кавычки!

---

**Готово!** Теперь пароль будет работать. 🔐

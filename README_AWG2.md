# 🚀 AWG-Easy 2.0

Web-интерфейс для управления AmneziaWG 2.0 VPN сервером.

## ✨ Что нового в версии 2.0

### AmneziaWG 2.0 Поддержка
- **S3, S4** - новые параметры обфускации
- **H1-H4** - теперь диапазоны `MIN-MAX` вместо одного числа
- **I5** - пятый спецпакет для имитации протоколов
- Улучшенная защита от DPI (Deep Packet Inspection)

### Базовый образ
```
amneziavpn/amneziawg-go:2.0.0
```

---

## 🎯 Быстрый старт

### Вариант 1: Автоматический запуск

```bash
# 1. Сборка Docker образа
./build.sh

# 2. Запуск с интерактивной настройкой
./run.sh
```

Скрипт автоматически:
- Определит ваш IP адрес
- Запросит пароль для Web UI
- Создаст и запустит контейнер
- Покажет ссылки для доступа

### Вариант 2: Docker Compose

```bash
# 1. Сборка образа
./build.sh

# 2. Редактирование docker-compose.yml
nano docker-compose.yml

# Обязательно укажите:
# - WG_HOST (ваш IP или домен)
# - PASSWORD_HASH (хеш пароля)

# 3. Запуск
docker-compose up -d
```

### Вариант 3: Ручной запуск

```bash
# Сборка
docker build -t awg-easy:2.0 .

# Генерация хеша пароля
docker run --rm awg-easy:2.0 wgpw 'your-password'

# Запуск
docker run -d \
  --name awg-easy \
  -e WG_HOST=your-server-ip \
  -e PASSWORD_HASH='$2y$05$...' \
  -e JC=5 \
  -e JMIN=9 \
  -e JMAX=47 \
  -e S1=9 \
  -e S2=128 \
  -e S3=5 \
  -e S4=16 \
  -e H1=1910617741-2036715259 \
  -e H2=2123686214-2136379622 \
  -e H3=2136749632-2137960503 \
  -e H4=2141598313-2146987659 \
  -v ~/.awg-easy:/etc/wireguard \
  -v ~/.awg-easy:/etc/amnezia/amneziawg \
  -p 51820:51820/udp \
  -p 51821:51821/tcp \
  --cap-add=NET_ADMIN \
  --cap-add=SYS_MODULE \
  --sysctl="net.ipv4.ip_forward=1" \
  --sysctl="net.ipv4.conf.all.src_valid_mark=1" \
  --device=/dev/net/tun:/dev/net/tun \
  --restart unless-stopped \
  awg-easy:2.0
```

---

## 📊 AmneziaWG 2.0 Параметры

### Обязательные параметры

| Переменная | Пример | Описание |
|------------|--------|----------|
| `WG_HOST` | `vpn.example.com` | Публичный IP или домен сервера |
| `PASSWORD_HASH` | `$2y$05$...` | Bcrypt хеш пароля Web UI |

### Параметры обфускации

| Параметр | Дефолт | Диапазон | Описание |
|----------|--------|----------|----------|
| **JC** | 5 | 3-10 | Количество junk пакетов |
| **JMIN** | 9 | 1-1000 | Минимальный размер junk пакета (байты) |
| **JMAX** | 47 | 1-1000 | Максимальный размер junk пакета (байты) |
| **S1** | 9 | 0-255 | Init packet junk size |
| **S2** | 128 | 0-255 | Response packet junk size |
| **S3** | 5 | 0-255 | Cookie reply junk size (🆕 AWG 2.0) |
| **S4** | 16 | 0-255 | Transport data junk size (🆕 AWG 2.0) |

### Magic Headers (диапазоны)

| Параметр | Пример | Описание |
|----------|--------|----------|
| **H1** | `1910617741-2036715259` | Magic header 1 (MIN-MAX) |
| **H2** | `2123686214-2136379622` | Magic header 2 (MIN-MAX) |
| **H3** | `2136749632-2137960503` | Magic header 3 (MIN-MAX) |
| **H4** | `2141598313-2146987659` | Magic header 4 (MIN-MAX) |

**Формат:** `MIN-MAX` где MIN и MAX - uint32 числа (0 до 4294967295)

**Важно:** Каждый пакет использует случайное значение из диапазона для максимальной рандомизации.

### Protocol Imitation (опционально)

| Параметр | Формат | Описание |
|----------|--------|----------|
| **I1** | `<b 0xHEX>` | Специальный пакет 1 |
| **I2** | `<b 0xHEX>` | Специальный пакет 2 |
| **I3** | `<b 0xHEX>` | Специальный пакет 3 |
| **I4** | `<b 0xHEX>` | Специальный пакет 4 |
| **I5** | `<b 0xHEX>` | Специальный пакет 5 (🆕 AWG 2.0) |
| **ITIME** | `0` | Задержка между I пакетами (ms) |

**Пример DNS пакета:**
```bash
I1='<b >'
```

---

## 📝 Примеры конфигурации

### Balanced (рекомендуется)

Баланс между защитой и производительностью:

```bash
JC=5
JMIN=9
JMAX=47
S1=9
S2=128
S3=5
S4=16
H1=1910617741-2036715259
H2=2123686214-2136379622
H3=2136749632-2137960503
H4=2141598313-2146987659
I1-I5=пустые
```

### Maximum Obfuscation

Максимальная защита от DPI:

```bash
JC=10
JMIN=50
JMAX=1000
S1=150
S2=150
S3=100
S4=100
H1=1000000000-2147483647
H2=1000000000-2147483647
H3=1000000000-2147483647
H4=1000000000-2147483647
I1=<DNS или QUIC пакет>
I2-I5=пустые
```

### Minimal Overhead

Минимальные накладные расходы:

```bash
JC=3
JMIN=9
JMAX=20
S1=5
S2=5
S3=0
S4=0
H1=1500000000-1600000000
H2=1600000000-1700000000
H3=1700000000-1800000000
H4=1800000000-1900000000
I1-I5=пустые
```

---

## 🔧 Управление

### Просмотр логов
```bash
docker logs -f awg-easy
```

### Перезапуск
```bash
docker restart awg-easy
```

### Остановка
```bash
docker stop awg-easy
```

### Удаление
```bash
docker stop awg-easy
docker rm awg-easy
```

### Обновление параметров

1. Остановите контейнер:
```bash
docker stop awg-easy
docker rm awg-easy
```

2. Запустите с новыми параметрами:
```bash
./run.sh
# или
docker-compose up -d
```

**Важно:** После изменения параметров нужно **пересоздать всех клиентов**, так как они должны получить новые настройки.

---

## 📱 Доступ к Web UI

После запуска контейнера:

1. Откройте браузер: `http://your-server-ip:51821`
2. Введите пароль
3. Создайте клиентов и скачайте конфигурации

### Создание клиента

1. Нажмите **"New Client"**
2. Введите имя клиента
3. Скачайте конфигурацию или отсканируйте QR код
4. Импортируйте в AmneziaVPN клиент

---

## 🔐 Безопасность

### Генерация хеша пароля

**⚠️ ВАЖНО:** Bcrypt хеш содержит символы `$`, которые нужно правильно экранировать!

#### Вариант 1: Использовать скрипт (рекомендуется)

```bash
./generate-password.sh
```

Скрипт покажет правильный формат для docker-compose.yml и docker run.

#### Вариант 2: Ручная генерация

```bash
# Генерация хеша
WGPW_OUTPUT=$(docker run --rm awg-easy:2.0 wgpw 'your-password')

# Извлечение хеша из вывода
HASH=$(echo "$WGPW_OUTPUT" | grep -oP "PASSWORD_HASH='\K[^']+")

echo $HASH
```

**Вывод (пример):**
```
$2y$12$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W
```

**Для docker-compose.yml:** Заменить все `$` на `$$`
```yaml
- PASSWORD_HASH=$$2y$$05$$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W
```

**Для docker run:** Использовать одинарные кавычки
```bash
-e PASSWORD_HASH='$2y$05$Gj8nULE.hqMWx2Bq0FjSiuMfDJQdqxD8H9hQ7oPYx3KcZGz8GvJ9W'
```

**Проверка:** После запуска контейнера
```bash
docker exec awg-easy printenv PASSWORD_HASH
# Должно вывести: $2y$05$Gj8n... (без экранирования)
```

❗ **Если пароль не подходит** - см. файл `PASSWORD_FIX.md` для подробного решения проблемы.

### Firewall

Откройте порты:
```bash
# WireGuard VPN
ufw allow 51820/udp

# Web UI (опционально, можно закрыть после настройки)
ufw allow 51821/tcp
```

---

## 📂 Структура данных

Все данные хранятся в примонтированном volume:

```
./data/
├── wg0.json       # Конфигурация сервера и клиентов
└── wg0.conf       # WireGuard конфиг (генерируется автоматически)
```

### Backup

```bash
# Создание бэкапа
tar -czf awg-easy-backup-$(date +%Y%m%d).tar.gz ./data/

# Восстановление
tar -xzf awg-easy-backup-YYYYMMDD.tar.gz
```

---

## 🐛 Troubleshooting

### Контейнер не запускается

Проверьте логи:
```bash
docker logs awg-easy
```

Типичные проблемы:
- Не указан `WG_HOST`
- Неверный формат `PASSWORD_HASH`
- Порты заняты

### Клиенты не подключаются

1. Проверьте что порт 51820/udp открыт:
```bash
netstat -uln | grep 51820
```

2. Проверьте IP forwarding:
```bash
sysctl net.ipv4.ip_forward
# Должно быть: net.ipv4.ip_forward = 1
```

3. Проверьте что контейнер запущен:
```bash
docker ps | grep awg-easy
```

### Неверный формат H параметров

H параметры должны быть в формате `MIN-MAX`:
```bash
# Правильно:
H1=1910617741-2036715259

# Неправильно:
H1=1910617741  # одно число (это AWG 1.0 формат)
```

---

## 📚 Дополнительная информация

### Клиенты AmneziaVPN

Скачать клиенты для разных платформ:
- [Официальный сайт](https://amnezia.org)
- [GitHub Releases](https://github.com/amnezia-vpn/amnezia-client/releases)
- [Google Play](https://play.google.com/store/apps/details?id=org.amnezia.vpn)

### Документация AmneziaWG

- [Официальная документация](https://docs.amnezia.org)
- [AmneziaWG 2.0 на GitHub](https://github.com/amnezia-vpn/amneziawg-go)

---

## 📄 Лицензия

Основано на [wg-easy](https://github.com/wg-easy/wg-easy)

Модифицировано для поддержки AmneziaWG 2.0

---

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте раздел Troubleshooting
2. Посмотрите логи: `docker logs awg-easy`
3. Проверьте что используете последнюю версию образа

---

**Версия:** AWG-Easy 2.0  
**Базовый образ:** amneziavpn/amneziawg-go:2.0.0  
**Протокол:** AmneziaWG 2.0

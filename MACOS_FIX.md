# 🍎 Исправлена проблема совместимости с macOS

## 🐛 Проблема

На **macOS** клиент WireGuard неправильно интерпретировал маску `/24`:

```ini
Address = 10.8.0.2/24
```

В результате интерфейс получал адрес **сети** вместо адреса **хоста**:

```bash
ifconfig utun10
# Было:
inet 10.8.0.0 --> 10.8.0.0 netmask 0xffffffff  # ❌ Неправильно!

# Должно быть:
inet 10.8.0.2 --> 10.8.0.2 netmask 0xffffffff  # ✅ Правильно!
```

Из-за адреса `10.8.0.0` (адрес сети) клиент не мог пинговать даже VPN сервер (`10.8.0.1`).

---

## ✅ Решение

Изменена маска подсети в клиентской конфигурации с `/24` на `/32`:

```diff
[Interface]
PrivateKey = ...
- Address = 10.8.0.2/24
+ Address = 10.8.0.2/32
DNS = 1.1.1.1,8.8.8.8
```

### Что изменено в коде:

**Файл:** `src/lib/WireGuard.js`, строка 260

```diff
- Address = ${client.address}/24
+ Address = ${client.address}/32
```

---

## 📝 Технические детали

### Что такое /32 и /24?

- `/32` - маска подсети для **одного хоста** (255.255.255.255)
- `/24` - маска подсети для **256 адресов** (255.255.255.0)

Для VPN клиента правильно использовать `/32`, так как:
1. Клиент получает один конкретный IP адрес
2. Маршрутизация происходит точечно (point-to-point)
3. Совместимость с разными ОС

### Почему macOS вел себя странно?

macOS WireGuard клиент при маске `/24` пытался вычислить адрес сети:
```
10.8.0.2/24 → Адрес сети: 10.8.0.0
```

И применял адрес сети вместо адреса хоста.

С маской `/32`:
```
10.8.0.2/32 → Адрес хоста: 10.8.0.2 (точно один адрес)
```

Проблема исчезает.

---

## 🧪 Проверка

После изменения клиентская конфигурация будет:

```ini
[Interface]
PrivateKey = ...
Address = 10.8.0.2/32  # ✅ Правильно для всех ОС
DNS = 1.1.1.1,8.8.8.8
# ... AWG параметры

[Peer]
PublicKey = ...
AllowedIPs = 0.0.0.0/0, ::/0
Endpoint = SERVER_IP:51820
```

### Проверка на macOS:

```bash
# Проверить адрес интерфейса
ifconfig utun10
# Должно показать: inet 10.8.0.2 --> 10.8.0.2

# Ping VPN сервера
ping 10.8.0.1
# Должен работать ✅

# Ping внешнего IP
ping 8.8.8.8
# Должен работать ✅

# Проверка вашего IP
curl ifconfig.me
# Должен показать IP сервера ✅
```

---

## 🎯 Совместимость

Маска `/32` работает корректно на всех платформах:

- ✅ **macOS** - теперь работает правильно
- ✅ **Windows** - работает
- ✅ **Linux** - работает
- ✅ **Android** - работает
- ✅ **iOS** - работает

---

## 📦 Что делать с существующими клиентами?

После обновления сервера:

### Вариант 1: Пересоздать клиентов (рекомендуется)

1. Удалить старых клиентов в Web UI
2. Создать заново
3. Скачать новые конфиги (уже с `/32`)

### Вариант 2: Ручное редактирование

В существующих конфигурациях изменить:

```diff
- Address = 10.8.0.2/24
+ Address = 10.8.0.2/32
```

И переподключиться.

---

## 🔍 История проблемы

**Симптомы:**
- ✅ Handshake успешен
- ✅ Status: Connected
- ❌ Ping не работает (даже до 10.8.0.1)
- ❌ Трафик не идет

**Диагностика показала:**
- ✅ IP forwarding включен
- ✅ MASQUERADE правила есть
- ✅ FORWARD правила есть
- ❌ Клиент получил адрес 10.8.0.0 (сеть) вместо 10.8.0.2 (хост)

**Решение:**
- Изменить маску с `/24` на `/32`

---

## ✅ Результат

После обновления:
- 🍎 macOS клиенты работают корректно
- 🔧 Все остальные платформы продолжают работать
- 📱 Улучшена совместимость
- 🎯 Правильная point-to-point конфигурация

---

**Версия:** Исправлено в AWG-Easy 2.0
**Дата:** 2025-01-21
**Платформа:** macOS (и улучшена совместимость для всех ОС)

# 🚨 Быстрое решение: Трафик не идет

## Проблема
Клиент подключен (Connected), но интернет не работает.

## ✅ Решение за 30 секунд

### Запустите диагностику:
```bash
./diagnose.sh
```

Скрипт автоматически:
- ✅ Проверит все настройки
- ✅ Исправит проблемы с iptables
- ✅ Покажет что тестировать

---

## 🔧 Ручное исправление

Если `diagnose.sh` не помогает:

### 1. Проверить IP forwarding
```bash
docker exec awg-easy sysctl net.ipv4.ip_forward
```

Должно быть `= 1`. Если `= 0`:
```bash
# Пересоздать контейнер
docker stop awg-easy
docker rm awg-easy
./run.sh
```

### 2. Добавить iptables правила вручную
```bash
# Определить интерфейс
INTERFACE=$(docker exec awg-easy ip route | grep default | awk '{print $5}')

# Добавить правила
docker exec awg-easy iptables -t nat -A POSTROUTING -s 10.8.0.0/24 -o $INTERFACE -j MASQUERADE
docker exec awg-easy iptables -A FORWARD -i wg0 -j ACCEPT
docker exec awg-easy iptables -A FORWARD -o wg0 -j ACCEPT
```

### 3. Перезапустить WireGuard
```bash
docker exec awg-easy wg-quick down wg0
docker exec awg-easy wg-quick up wg0
```

---

## 🧪 Проверка с клиента

После исправления:

```bash
# 1. Ping VPN сервера
ping 10.8.0.1

# 2. Ping Google DNS
ping 8.8.8.8

# 3. Проверка DNS
ping google.com

# 4. Проверка IP
curl ifconfig.me
# Должен показать IP сервера
```

Если всё пингуется - **трафик работает!** ✅

---

## 📚 Подробности

Полная документация: **TRAFFIC_NOT_WORKING_FIX.md**

---

**Самое частое:** IP forwarding отключен. Просто пересоздайте контейнер через `./run.sh`!
